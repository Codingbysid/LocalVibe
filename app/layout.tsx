import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LocalVibe - AI-Curated Local Experiences',
  description: 'Transform urban discovery with personalized, AI-curated Vibe Trails. Stop searching, start experiencing your city.',
  keywords: 'local discovery, AI recommendations, city exploration, vibe trails, urban experiences',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
          {children}
        </div>
      </body>
    </html>
  )
}
